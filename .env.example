# Standard MERN environment variables
# -----------------------------------
# MongoDB connection string (used by backend)
MONGO_URI=mongodb://appuser:pass@localhost:27017/employees?authSource=employees
# JWT secret for authentication (used by backend)
JWT_SECRET=your-jwt-secret-here
# Port for backend server
PORT=5000
# Node environment
NODE_ENV=development
# URL for frontend (used for CORS, etc.)
CLIENT_URL=http://localhost:3000

# Docker/Compose specific (optional, override as needed)
ADMIN_USER=admin
ADMIN_PASS=g22
APP_DB_NAME=employees
APP_DB_USERNAME=appuser
APP_DB_PASSWORD=pass
MONGO_PORT=27017
BACKEND_PORT=5000
FRONTEND_PORT=3000

# Kubernetes: set these as secrets or configmaps as appropriate
# (see infra/k8s/ for deployment YAMLs)

# Notes:
# - For Docker Compose, these variables are set in docker-compose.yml
# - For Kubernetes, set as env in deployment YAMLs or use secrets/configmaps
# - For local dev, copy this file to .env and fill in real values
