{
  "name": "g22-mern-app",
  "version": "1.0.0",
  "description": "MERN Stack Application",
  "scripts": {
    "install:all": "npm --prefix server install && npm --prefix frontend install",
    "dev": "concurrently \"npm --prefix server run dev\" \"npm --prefix frontend run dev\"",
    "build": "npm --prefix server run build || true && npm --prefix frontend run build",
    "start": "npm --prefix server start",
    "lint": "eslint \"server/**/*.{js,ts,tsx}\" \"frontend/**/*.{js,ts,tsx}\"",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "prettier": "^3.3.3",
    "eslint": "^9.12.0"
  },
  "keywords": ["mern", "react", "node", "express", "mongodb"],
  "author": "Stryker76",
  "license": "MIT"
}